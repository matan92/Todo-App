# TodoApp (Python + Tkinter + Firebase)

A simple desktop to-do app with:
- Add / Delete (with confirmation)
- Edit on double-click
- Real checkboxes with `ttk`
- Search bar filter
- Firebase Realtime Database sync (add / toggle / delete)
